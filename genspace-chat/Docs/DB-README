The genSpace database must have the following stored procedures created in order for the 
Tigase XMPP server to be able to properly authenticate against it:

create procedure dbo.TigUserLoginPlainPw 
	@user varchar(50),
	@pass varchar(50)
as
SET NOCOUNT OFF
select username+'@lenox.cs.columbia.edu' from registration where username=replace(@user,'@lenox.cs.columbia.edu','') and password=SUBSTRING(master.dbo.fn_varbintohexstr(HashBytes('sha1', @pass)), 3, 42)



create procedure dbo.TigUserLogout
	@user varchar(50)
as
select 1